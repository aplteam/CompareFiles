 r←Initial;msg;folder
 msg←'These tests require the version to be tested to be the official user command.'
 :If ∆YesOrNo msg,' Would you like to carry on?'
     r←1
     folder←'TestData\'
     folder{( ⍺,⍵,'.txt')⎕NCOPY⍠('IfExists' 'Replace')⊣ ⍺,⍵,'.orig'}'file1 with blank'
     folder{(⍺,⍵,'.txt')⎕NCOPY ⍠('IfExists' 'Replace')⊣⍺,⍵,'.orig' }'file2 with blank'
 :Else
     r←0
 :EndIf
⍝Done
