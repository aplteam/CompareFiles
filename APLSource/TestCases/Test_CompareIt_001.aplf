 R←Test_CompareIt_001(stopFlag batchFlag);⎕TRAP;msg;r;tool
⍝ Run "CompareIt" with defaults: no file can be edited.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :If batchFlag
     →0,R←T._NoBatchTest
 :EndIf

 msg←⊂⊃⎕SI
 msg,←⊂' This test case needs you: it fires up CompareIt.'
 msg,←⊂' Both files should NOT be editable.'
 ∆Show msg

 r←⎕SE.UCMD'CompareFiles ''TestData\file1 with blank.txt'' ''TestData\file2 with blank.txt'' -use=CompareIt'

 :If ∆YesOrNo'Was "CompareIt" started and no file was editable?'
 :AndIf 2=+/r
     R←T._OK
 :EndIf
