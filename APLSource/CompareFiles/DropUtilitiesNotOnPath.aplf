 (exes_ names_)←DropUtilitiesNotOnPath(exes names);paths;exe;b
⍝ Checks all folders found on `PATH` for hosting `exes`.
⍝ Add the path when found but replace with '' otherwise
 paths←';'(≠⊆⊢)2 ⎕NQ #'GetEnvironment' 'PATH'
 paths/⍨←~':\WINDOWS'{b←':\WINDOWS'{⍺∘≡¨(≢⍺)↑¨1↓¨⍵}paths ⋄ b∧(⊃¨(1+≢⍺)↓¨⍵)∊¨⊂' \'}paths
 exes_←names_←''
 :For exe :In exes
     :If ∨/b←F.IsFile paths,¨⊂'/',exe
         exes_,←⊂((b⍳1)⊃paths),'/',exe
         names_,←⊂(exes⍳⊂exe)⊃names
     :EndIf
 :EndFor
⍝Done
